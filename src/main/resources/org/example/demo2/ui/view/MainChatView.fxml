<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.*?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.shape.*?>

<BorderPane styleClass="messenger-root" stylesheets="@../css/theme.css,@../css/messenger.css" xmlns="http://javafx.com/javafx/17.0.2-ea" xmlns:fx="http://javafx.com/fxml/1" fx:controller="org.example.demo2.ui.controller.MainChatController">

    <!-- Left Sidebar: Friends/Groups List -->
    <left>
        <VBox minWidth="260" maxWidth="400" prefWidth="320" styleClass="sidebar-left">
            <!-- Header: Title + New Message -->
            <HBox alignment="CENTER_LEFT" spacing="12" minHeight="50" prefHeight="50" styleClass="sidebar-header">
                <padding>
                    <Insets bottom="10" left="14" right="14" top="14" />
                </padding>
                <!-- App Name -->
                <Label style="-fx-text-fill: #ffffff; -fx-font-size: 22px; -fx-font-weight: bold;" text="AegisTalk" />
                <Region HBox.hgrow="ALWAYS" />
            </HBox>

            <!-- Search Bar -->
            <HBox minHeight="50" prefHeight="50" styleClass="search-container">
                <padding>
                    <Insets bottom="10" left="14" right="14" top="8" />
                </padding>
                <TextField fx:id="txtSearch" minHeight="40" prefHeight="40" maxHeight="44" promptText="🔍  Tìm kiếm..." styleClass="search-input" HBox.hgrow="ALWAYS" />
            </HBox>

            <!-- Search Results Dropdown (ẩn mặc định) -->
            <VBox fx:id="searchResultsContainer" managed="false" styleClass="search-results" visible="false">
                <padding>
                    <Insets bottom="12" left="20" right="20" top="0" />
                </padding>
                <ListView fx:id="lstSearchResults" prefHeight="200" styleClass="search-results-list" />
            </VBox>

            <!-- Tabs: Bạn bè - Nhóm - Lời mời -->
            <HBox alignment="CENTER_LEFT" spacing="8" minHeight="48" prefHeight="48" styleClass="tabs-container">
                <padding>
                    <Insets bottom="6" left="16" right="16" top="6" />
                </padding>
                <ToggleButton fx:id="tabFriends" minWidth="110" prefWidth="120" styleClass="tab-button" text="💬 Tất cả" />
                <ToggleButton fx:id="tabGroups" minWidth="100" prefWidth="110" styleClass="tab-button" text="👥 Nhóm" />
                <ToggleButton fx:id="tabRequests" minWidth="140" prefWidth="150" styleClass="tab-button" text="📨 Lời mời" />
                <Region HBox.hgrow="ALWAYS" />
                <Button fx:id="btnCreateGroup" minHeight="36" minWidth="36" prefHeight="36" prefWidth="36" onAction="#handleCreateGroup" styleClass="icon-button" text="➕">
                    <tooltip>
                        <Tooltip text="Tạo nhóm mới" />
                    </tooltip>
                </Button>
            </HBox>

            <!-- Friends/Groups List -->
            <ListView fx:id="lstContacts" styleClass="contacts-list" VBox.vgrow="ALWAYS" />

            <!-- User Profile Section -->
            <HBox alignment="CENTER_LEFT" spacing="10" minHeight="75" prefHeight="75" styleClass="user-profile-section">
                <padding>
                    <Insets bottom="10" left="14" right="14" top="10" />
                </padding>
                <StackPane>
                    <Circle fx:id="currentUserAvatar" radius="22" />
                    <Circle radius="6" styleClass="online-indicator" translateX="15" translateY="15" />
                </StackPane>
                <VBox spacing="3" HBox.hgrow="ALWAYS">
                    <Label fx:id="lblCurrentUser" style="-fx-font-size: 14px;" styleClass="chat-name" text="User" />
                    <Label style="-fx-font-size: 12px;" styleClass="chat-status" text="Đang hoạt động" />
                </VBox>
                <Button fx:id="btnToggleNotification" onAction="#handleToggleNotification" minHeight="38" minWidth="38" prefHeight="38" prefWidth="38" maxHeight="42" maxWidth="42" styleClass="icon-button" text="🔔">
                    <tooltip>
                        <Tooltip text="Bật/Tắt thông báo" />
                    </tooltip>
                </Button>
                <Button fx:id="btnEditProfile" onAction="#handleEditProfile" minHeight="38" minWidth="38" prefHeight="38" prefWidth="38" maxHeight="42" maxWidth="42" styleClass="icon-button" text="✏️">
                    <tooltip>
                        <Tooltip text="Cập nhật hồ sơ" />
                    </tooltip>
                </Button>
                <Button fx:id="btnLogout" onAction="#handleLogout" minHeight="38" minWidth="38" prefHeight="38" prefWidth="38" maxHeight="42" maxWidth="42" styleClass="icon-button" text="🚪">
                    <tooltip>
                        <Tooltip text="Đăng xuất" />
                    </tooltip>
                </Button>
            </HBox>
        </VBox>
    </left>

    <!-- Center: Chat Area -->
    <center>
        <VBox styleClass="chat-center">
            <!-- Chat Header -->
            <HBox alignment="CENTER_LEFT" spacing="12" styleClass="chat-header">
                <padding>
                    <Insets bottom="12" left="16" right="16" top="12" />
                </padding>

                <!-- Avatar -->
                <StackPane>
                    <Circle fx:id="avatarCircle" radius="24" styleClass="avatar-circle" />
                    <Circle fx:id="statusIndicator" radius="7" styleClass="online-indicator" translateX="17" translateY="17" />
                </StackPane>

                <!-- Name + Status -->
                <VBox spacing="4">
                    <Label fx:id="lblChatName" styleClass="chat-name" />
                    <Label fx:id="lblChatStatus" styleClass="chat-status" text="" />
                </VBox>

                <Region HBox.hgrow="ALWAYS" />

                <!-- Action Buttons -->
                <HBox spacing="10">
                    <Button fx:id="btnVideoCall" minHeight="40" minWidth="40" prefHeight="44" prefWidth="44" maxHeight="48" maxWidth="48" styleClass="icon-button" text="📹">
                        <tooltip>
                            <Tooltip text="Video call" />
                        </tooltip>
                    </Button>
                    <Button fx:id="btnInfo" minHeight="40" minWidth="40" prefHeight="44" prefWidth="44" maxHeight="48" maxWidth="48" styleClass="icon-button" text="ℹ">
                        <tooltip>
                            <Tooltip text="Thông tin cuộc chat" />
                        </tooltip>
                    </Button>
                </HBox>
            </HBox>

            <!-- Messages Area -->
            <ScrollPane fx:id="scrollMessages" fitToWidth="true" styleClass="messages-scroll" VBox.vgrow="ALWAYS">
                <VBox fx:id="messagesContainer" spacing="12" styleClass="messages-container">
                    <padding>
                        <Insets bottom="20" left="24" right="24" top="20" />
                    </padding>

                    <!-- Empty State (shown when no messages) -->
                    <VBox fx:id="emptyState" alignment="CENTER" spacing="16" styleClass="empty-state">
                        <Label styleClass="empty-state-icon" text="💬" />
                        <Label styleClass="empty-state-title" text="Bắt đầu cuộc trò chuyện" />
                        <Label alignment="CENTER" maxWidth="300" styleClass="empty-state-subtitle" text="Chọn một liên hệ từ danh sách bên trái để bắt đầu nhắn tin" wrapText="true" />
                    </VBox>
                </VBox>
            </ScrollPane>

            <!-- Input Area -->
            <HBox alignment="CENTER" spacing="12" styleClass="input-area">
                <padding>
                    <Insets bottom="18" left="20" right="20" top="16" />
                </padding>

                <!-- Attachment Button -->
                <Button fx:id="btnAttach" onAction="#handleAttachFile" minHeight="40" minWidth="40" prefHeight="44" prefWidth="44" maxHeight="48" maxWidth="48" styleClass="icon-button" text="📎">
                    <tooltip>
                        <Tooltip text="Đính kèm file" />
                    </tooltip>
                </Button>

                <!-- Image Button -->
                <Button fx:id="btnImage" onAction="#handleAttachImage" minHeight="40" minWidth="40" prefHeight="44" prefWidth="44" maxHeight="48" maxWidth="48" styleClass="icon-button" text="🖼️">
                    <tooltip>
                        <Tooltip text="Gửi hình ảnh" />
                    </tooltip>
                </Button>

                <!-- Message Input -->
                <TextField fx:id="txtMessage" minHeight="44" prefHeight="48" maxHeight="52" promptText="Nhập tin nhắn..." styleClass="message-input" HBox.hgrow="ALWAYS" />

                <!-- Send Button -->
                <Button fx:id="btnSend" minHeight="44" prefHeight="48" maxHeight="52" styleClass="send-button" text="Gửi ➤" />
            </HBox>
        </VBox>
    </center>

    <!-- Right Sidebar: Info Panel (ẩn mặc định) -->
    <right>
        <VBox fx:id="infoPanel" managed="false" minWidth="260" maxWidth="400" prefWidth="340" styleClass="sidebar-right" visible="false">
            <padding>
                <Insets bottom="20" left="20" right="20" top="20" />
            </padding>

            <!-- Close Button -->
            <HBox alignment="CENTER_RIGHT">
                <Button fx:id="btnCloseInfo" prefHeight="36" prefWidth="36" styleClass="icon-button" text="✕" />
            </HBox>

            <!-- User/Group Info -->
            <VBox alignment="CENTER" spacing="20">
                <padding>
                    <Insets bottom="24" top="12" />
                </padding>

                <StackPane>
                    <Circle fx:id="infoAvatar" radius="56" styleClass="info-avatar" />
                </StackPane>
                <VBox alignment="CENTER" spacing="6">
                    <Label fx:id="lblInfoName" styleClass="info-name" text="" />
                    <HBox alignment="CENTER" spacing="6">
                        <Circle radius="5" styleClass="online-indicator" />
                        <Label fx:id="lblInfoStatus" styleClass="info-status" text="Đang hoạt động" />
                    </HBox>
                </VBox>
            </VBox>

            <!-- Quick Action Buttons -->
            <HBox alignment="CENTER" spacing="12">
                <padding>
                    <Insets bottom="24" />
                </padding>
                <VBox alignment="CENTER" spacing="6">
                    <Button fx:id="btnInfoVideoCall" onAction="#handleVideoCall" prefHeight="48" prefWidth="48" styleClass="icon-button" text="📹" />
                    <Label styleClass="muted-text" text="Video" />
                </VBox>
            </HBox>

            <!-- Search Messages Panel (hidden by default) -->
            <VBox fx:id="searchMessagesPanel" managed="false" spacing="12" visible="false">
                <HBox alignment="CENTER_LEFT" spacing="10">
                    <TextField fx:id="txtSearchMessages" onAction="#performMessageSearch" prefHeight="40" promptText="Tìm tin nhắn..." styleClass="search-input" HBox.hgrow="ALWAYS" />
                    <Button fx:id="btnCloseSearch" onAction="#closeMessageSearch" prefHeight="36" prefWidth="36" styleClass="icon-button" text="✕" />
                </HBox>
                <Label fx:id="lblMessageSearchResults" styleClass="muted-text" text="" />
                <ListView fx:id="lstMessageSearchResults" managed="false" prefHeight="200" styleClass="search-results-list" visible="false" />
            </VBox>

            <!-- Action Buttons -->
            <VBox spacing="10">
                <Button fx:id="btnShowFiles" onAction="#handleShowFiles" maxWidth="Infinity" prefHeight="44" styleClass="info-button" text="📁  File &amp; phương tiện" />
            </VBox>

            <!-- Info Sections -->
            <VBox spacing="12" VBox.vgrow="ALWAYS">
                <padding>
                    <Insets top="20" />
                </padding>
                <Label styleClass="info-section-title" text="QUYỀN RIÊNG TƯ" />
                <VBox spacing="4">
                    <Button fx:id="btnMuteConversation" alignment="CENTER_LEFT" maxWidth="Infinity" onAction="#handleMuteCurrentConversation" style="-fx-background-color: transparent; -fx-cursor: hand;" styleClass="info-item" text="🔔  Tắt thông báo" />
                    <Button fx:id="btnUnfriend" alignment="CENTER_LEFT" maxWidth="Infinity" onAction="#handleUnfriend" style="-fx-background-color: transparent; -fx-cursor: hand;" styleClass="info-item" text="❌  Huỷ kết bạn" />
                </VBox>
            </VBox>
        </VBox>
    </right>

</BorderPane>
